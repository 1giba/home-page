/**
 * PostCSS configuration for Next.js with Tailwind CSS
 * Using the correct plugin name '@tailwindcss/postcss' for Next.js 15
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};

export default config;
